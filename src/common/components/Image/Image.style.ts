import { LazyLoadImage } from "react-lazy-load-image-component";
import styled from "styled-components";

interface Props {}

export const StyledLazyImage = styled(LazyLoadImage)<Props>``;

export const StyledImage = styled.img<Props>``;
